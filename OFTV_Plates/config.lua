Config = {}

-- Default plate image (without extension)
Config.DefaultPlate = "regular_tx"

-- List of custom plates
Config.CustomPlates = {
    ["regular_tx"] = "Texas Plate",
    ["template_plate"] = "Template Plate"
}

-- Apply to NPC vehicles automatically
Config.ApplyToNPCs = true
